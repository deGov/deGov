(function ($, Drupal) {

  /*
  This is what a behavior should look like:
  Drupal.behaviors.custom_behavior = {
    attach: context => {
      $(context).find('.le-selector').once('custom_behavior').each(function () {
        // ... le code
      });
    }
  };*/
})(jQuery, window.Drupal);
